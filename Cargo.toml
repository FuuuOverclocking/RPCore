[package]
name = "rpcore"
version = "0.1.0"
edition = "2021"

[dependencies]
crossbeam = { version = "0.8.4", optional = true }
event-manager = { version = "0.4.0", optional = true }
log = "0.4.25"
nix = { version = "0.29.0", features = ["event"] }
oneshot = "0.1.10"
